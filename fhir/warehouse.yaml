apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: fhir-warehouse
  namespace: fhir
spec:
  subscriptions:
    - image:
        repoURL: ghcr.io/gemas/guestbook
        imageSelectionStrategy: SemVer
